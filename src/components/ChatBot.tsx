import React from 'react';

const ChatBot = () => {
    return (
        <div className="fixed bottom-4 right-4 bg-primary text-white p-3 rounded-full cursor-pointer shadow-lg hover:bg-primary/90">
            Chat
        </div>
    );
};

export default ChatBot;
